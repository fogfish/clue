{erl_opts, [
   %native, {hipe, o3}
]}.

{lib_dirs, [".", "deps"]}.
{cover_enabled, true}.

{deps, [
   % erlang toolkit
   {feta, ".*",
      {git, "http://github.com/fogfish/feta", {branch, master}}
   }
   
   % http stream parser
  ,{htstream, ".*",
      {git, "http://github.com/kfsm/htstream", {branch, master}}
   }
]}.
