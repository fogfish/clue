{erl_opts, [
   %native, {hipe, o3}
]}.

{lib_dirs, [".", "deps"]}.
{cover_enabled, true}.

{deps, [
   % erlang toolkit
   {feta, ".*",
      {git, "https://github.com/fogfish/feta", {branch, master}}
   }
]}.
